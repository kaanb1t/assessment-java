public class Developer extends Employee {
    private boolean mentor;

    public Developer(String employeeId, String name, boolean isContractor) {
        super(employeeId, name, isContractor);
    }
}
