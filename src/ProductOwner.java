public class ProductOwner extends Employee {
    private boolean certified;

    public ProductOwner(String employeeId, String name, boolean isContractor) {
        super(employeeId, name, isContractor);
    }
}